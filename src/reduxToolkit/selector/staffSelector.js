export const isLogin = (state) => state.staff.isLoginStaff;
export const notification = (state) => state.staff.notification;
export const userInfo = (state) => {
   return state.staff.infoUser;
};

export const listUser = (state) => state.staff.listUser;
export const isLoading = (state) => state.staff.isLoading;
