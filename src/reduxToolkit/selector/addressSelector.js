export const listCityItem = (state) => state.address.listCity;
export const cityInfo = (state) => state.address.city;

export const listDistrictItem = (state) => state.address.listDistrict;
export const districtInfo = (state) => state.address.district;

export const listWardItem = (state) => state.address.listWard;
export const wardInfo = (state) => state.address.ward;
